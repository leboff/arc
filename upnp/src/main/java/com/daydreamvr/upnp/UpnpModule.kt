package com.daydreamvr.upnp

/**
 * Phase 1 placeholder.
 *
 * The SSDP client, device-description parser, SOAP/DIDL-Lite layer and
 * [com.daydreamvr.upnp] public surface land in Phase 3 (docs/PLAN.md §3). This
 * module is intentionally a `java-library` with no `android.*` on the classpath;
 * the `checkNoAndroidImports` verification task keeps it that way.
 */
internal object UpnpModule
