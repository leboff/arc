package com.daydreamvr.playback

/**
 * Phase 1 placeholder.
 *
 * [VideoPlayer], the ExoPlayer wrapper, resume logic and the scrub controller
 * land in Phase 4 (docs/PLAN.md §4).
 */
internal object PlaybackModule
